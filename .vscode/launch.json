{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome localhost",
            "type": "pwa-chrome",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}/public/js",
            "outputCapture": "std"
        }
    ]
}